import { Box } from '@mui/material';
import React from 'react';

const Forum = () => {
    return (
        <Box
            sx={{
                width: '100%',
                maxWidth: 1200,
                textAlign: 'center',
                minHeight: 780,
            }}
        >
            Forum
        </Box>
    );
};

export default Forum;
