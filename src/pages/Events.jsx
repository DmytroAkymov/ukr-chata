import { Box } from '@mui/material';
import React from 'react';

const Events = () => {
    return (
        <Box
            sx={{
                width: '100%',
                maxWidth: 1200,
                textAlign: 'center',
                minHeight: 780,
            }}
        >
            Events
        </Box>
    );
};

export default Events;
