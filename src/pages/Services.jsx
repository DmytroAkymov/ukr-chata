import { Box } from '@mui/material';
import React from 'react';

const Services = () => {
    return (
        <Box
            sx={{
                width: '100%',
                maxWidth: 1200,
                textAlign: 'center',
                minHeight: 780,
            }}
        >
            Services
        </Box>
    );
};

export default Services;
